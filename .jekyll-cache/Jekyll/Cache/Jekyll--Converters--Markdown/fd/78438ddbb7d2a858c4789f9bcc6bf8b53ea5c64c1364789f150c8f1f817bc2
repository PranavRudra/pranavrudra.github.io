I"<h2 id="solution">Solution:</h2>

<p>We will construct $f(n), g(n)$ such that $f(n) &gt; g(n)$ for $n \equiv 0 \pmod{5}$ and $g(n) &gt; f(n)$ for all other $n$. To do so, we will define each function piecewise.</p>

\[f(n) = \begin{cases}
        1, &amp; \text{if $n \equiv 0 \pmod{5}$}\\
        0, &amp; \text{if $n \not\equiv 0 \pmod{5}$}
    \end{cases}\]

\[f(n) = \begin{cases}
        0, &amp; \text{if $n \equiv 0 \pmod{5}$}\\
        0, &amp; \text{if $n \not\equiv 0 \pmod{5}$}
    \end{cases}\]

\[\qed\]
:ET