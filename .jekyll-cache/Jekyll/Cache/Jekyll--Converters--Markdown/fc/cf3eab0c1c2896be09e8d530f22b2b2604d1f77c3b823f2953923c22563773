I"Á<h2 id="solution">Solution:</h2>

<p>Let $P(n)$ be a polynomial in $n$ of degree $k$. That is,</p>

\[P(n) = a_kn^k + a_{k - 1}n^{k - 1} + \cdots + a_1n + a_0\]

<p>where $k \in \mathbb{N}$ and $a_i \in \mathbb{R}^{\geq 0}$ for $i = 0, 1, 2, \ldots, k$. Also, let</p>

\[Q(n) = \left[1 + \left( \sum_{i = 0}^{k} a_i \right) \right] n^{k!}\]

<p>$\forall n \geq 1$, clearly $0 \leq P(n) \leq Q(n)$. Thus, $\log_b(P(n)) \leq \log_b(Q(n))$ $\forall n \geq 1, b \in \mathbb{R}^+$.</p>

<p>$$
    \begin{align*}
        \log_b(Q(n)) &amp;= \log_b \left( \left[1 + \left( \sum_{i = 0}^{k} a_i \right) \right] n^{k!} \right) \</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>\end{align*} $$ \log_b(Q(n)) = \log_b \left( \left[1 + \left( \sum_{i = 0}^{k} a_i \right) \right] n^{k!} \right) = \log_b \left( \left[1 + \left( \sum_{i = 0}^{k} a_i \right) \right] \right) + k!\log_b(n)$$
</code></pre></div></div>
:ET